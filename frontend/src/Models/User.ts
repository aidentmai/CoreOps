export type UserProfileToken = {
    name: any;
    id: string;
    userName: string;
    email: string;
    token: string;
}

export type UserProfile = {
    id: string;
    userName: string;
    email: string;
}